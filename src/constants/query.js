const COOKIE = {
  COOKIE_NAME: 'myToken',
  REFRESH_NAME: 'myRefreshToken',
};

const AXIOS_PATH = {
  LOCAL: 'http://localhost:4000',
  HOST: 'http://localhost:4000',
  POST: '/post',
};

const STALETIME = {
  FIVE_MIN: 5 * 60 * 1000,
};

const KEY = {
  POSTS: 'posts',
};

const QUERY = {
  AXIOS_PATH,
  COOKIE,
  STALETIME,
  KEY,
};

export default QUERY;
